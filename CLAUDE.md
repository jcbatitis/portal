# CLAUDE.md - AI Assistant Guide for `portal`

## Repository Overview

This is the **portal** project repository. It is currently in its initial state with no application code, build system, or dependencies configured yet.

- **Repository name:** portal
- **Owner:** jcbatitis
- **Current state:** Freshly initialized (README.md only)

## Project Structure

```
portal/
├── CLAUDE.md          # This file - AI assistant guide
└── README.md          # Project README (placeholder)
```

## Development Setup

No development environment has been configured yet. When the project is initialized, update this section with:

- Language and runtime requirements (e.g., Node.js version, Python version)
- Package manager (npm, yarn, pnpm, pip, etc.)
- Installation steps (`npm install`, etc.)
- Environment variables needed (document in `.env.example`)

## Common Commands

<!-- Update this section as commands are established -->

```bash
# No commands configured yet. Examples to add:
# npm install        - Install dependencies
# npm run dev        - Start development server
# npm run build      - Production build
# npm run test       - Run test suite
# npm run lint       - Run linter
```

## Architecture

No application architecture has been established yet. When code is added, document:

- Framework and major libraries
- Directory structure conventions
- State management approach
- API/data layer patterns
- Routing strategy

## Code Conventions

When contributing to this project, follow these guidelines:

- Write clear, descriptive commit messages
- Keep pull requests focused on a single concern
- Add tests for new functionality
- Do not commit secrets, credentials, or `.env` files

## Testing

No test framework has been configured yet. When tests are added, document:

- Test framework and runner
- How to run tests (`npm test`, etc.)
- Test file naming conventions (e.g., `*.test.ts`, `*.spec.ts`)
- Test directory structure

## CI/CD

No CI/CD pipeline has been configured yet. When added, document:

- Pipeline tool (GitHub Actions, etc.)
- Required checks before merge
- Deployment process

## Notes for AI Assistants

- This repository is in its initial state. Expect the structure to evolve significantly.
- Always read files before modifying them.
- When adding new project scaffolding, prefer widely-adopted, well-maintained tools.
- Update this CLAUDE.md file as the project structure and conventions are established.
